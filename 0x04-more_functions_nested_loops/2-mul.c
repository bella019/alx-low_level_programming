#include "main.h"
/**
 * mul - multiplies two intergers
 * @a: to be multplied with b
 * @b: to be multplied with a
 * Return: the reult of the multplication operations"
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
